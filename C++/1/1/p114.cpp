#include<iostream>
using namespace std;
int main()
{
	/*char answer;
	while (1){
		do{
			cout << "계속 진행 하시겠습니까 ? " << endl;
			cout << "계속하려면 'Y'를 중지하려면 'N'을 누르세요." << endl;
			cin >> answer;

		} while ((toupper(answer != 'Y')) && (toupper(answer != 'N')));

		if ((toupper(answer) == 'Y'))
		{
			cout << "계속 진행 합니다" << endl;
			continue;
		}
		else
		{
			cout << "중지합니다" << endl;
			break;
		}
	}*/

	char a;
	cin >> a;
	cout << a << endl;
	cout << a * 1;
		return 0;
	
}